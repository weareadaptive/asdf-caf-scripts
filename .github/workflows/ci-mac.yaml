---
name: Mac test

on:
  workflow_dispatch:
  push:
    branches: ["*"]

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  create-local-k8s:
    runs-on: macos-13
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download and Install OrbStack via Brew
        run: |
          curl -sLo orbstack.rb https://raw.githubusercontent.com/Homebrew/homebrew-cask/65da904d37b45eb3f0644e6419ebde37ef927f20/Casks/o/orbstack.rb
          brew install --cask ./orbstack.rb
          orbctl --help

      # - name: Download and Install OrbStack DMG file
      #   run: |
      #     TMPMOUNT=$(/usr/bin/mktemp -d /tmp/orbstack.XXXX)
      #     mkdir -p "$TMPMOUNT/downloads"
      #     curl -Lso $TMPMOUNT/downloads/OrbStack.dmg https://cdn-updates.orbstack.dev/amd64/OrbStack_v1.5.1_16857_amd64.dmg
      #     hdiutil attach $TMPMOUNT/downloads/OrbStack.dmg -mountpoint $TMPMOUNT
      #     pushd $TMPMOUNT
      #     tar cf - "./OrbStack.app" | tar xf - -C /Applications
      #     popd
      #     hdiutil detach $TMPMOUNT
      #     orb start

      - name: Install asdf for macOS
        run: |
          git clone https://github.com/asdf-vm/asdf.git ~/.asdf --branch v0.14.1
          echo '. $HOME/.asdf/asdf.sh' >> ~/.zshrc
          source ~/.zshrc
          asdf plugin add caf-scripts https://github.com/AdaptiveConsulting/asdf-caf-scripts.git
          echo .tool-versions
          cut -d' ' -f1 .tool-versions | xargs -I{} asdf plugin-add {} || true
          asdf install
          caf_create_local_k8s.sh


      # - name: Create Local K8s
      #   run: |
      #     caf_create_local_k8s.sh

      # - name: Route to Local K8s
      #   run: |
      #     caf_route_to_local_k8s.sh

      # - name: Test local meta charts
      #   run: |
      #     direnv exec . which caf_create_local_k8s.sh
